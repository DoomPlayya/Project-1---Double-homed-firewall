---
- name: uppdatera apt-cache och installera basverktyg för felsökning    
  apt:
    name:
      - curl
      - net-tools
      - iputils-ping
      - vim
      - traceroute
    state: present
    update_cache: yes
    cache_valid_time: 3600
  become: yes