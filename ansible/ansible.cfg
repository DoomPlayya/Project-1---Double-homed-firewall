[defaults]
inventory = inventory.ini
remote_user = vagrant
host_key_checking = False
private_key_file = {{CWD}}/.vagrant/machines/firewall/virtualbox/private_key

roles_path = ./roles
stdout_callback = yaml

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False